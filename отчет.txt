Подключаю необходимые библиотеки,
создаю структуру
Создаю файлы(бинарные)

Объявляю структуру, и массив из структур.
Объявляю функции

С консоли размерность нашей структуры(массив эелементов)
Проверяю ее на верность введеных данных, необходимо ввести только целочисленное значение не больше 20.
далее инциализирую(заполняю) структуру введенными данными.
складываю все в файл.
затем считываю информацию с файла
сортирую данные, для лучшей наглядности методом пузырька
Вывожу введенные отсортированные по возрастанию численнсоти данные

Прошу пользователя указать минмальную численность
Пробегаюсь по массиву всей структуры, ищу и сравниваю значение с минмальной численностью, если нашлось забираю номер элемента массива структуры, и уже с него перезаменяю данные на следующий массив, тоесть если 1 массив попал под условие , то данные 2 массива присваиваю первому массиву.
Остальное оставляю без изменений.
Вывожу отсортированный, отредактированный массив структур, сортировка сделана для наглядности

Добавляю новый элемент массива после введенного элемента массива
Выделяю место для нового элемента структуры
Проверяю, верно ли введено число(эсть ли такой элемент в структуре) К примеру: если элемента 2 а ввели число 3, то мы не может добавить новый элемент после такого числа, т.к 3 структуры нет.
Далее, беру введенный элемент и присваиваю данные в предыдущий элемент, тем самым освобождаю новый элемент и вставляю новые данные в этот элемент.
Сохраняю всю структуру в новый отдельный двоичный файл.





